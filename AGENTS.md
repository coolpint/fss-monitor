# AGENTS.md

이 저장소에서 작업하는 모든 에이전트는 아래 기준을 따른다.

- 사용자와 대화할 때는 존대말을 사용한다.
- 파일 삭제가 필요하면 `rm` 대신 항상 `trash`를 사용한다. `rm`을 폴백으로 쓰지 않는다.
- 중요한 작업 전에는 `plan.md`와 `constitution.md`를 먼저 확인한다.
- 의미 있는 기능 변경, 운영 방식 변경, 일정/알림/자동화 변경, 외부 서비스 연동 변경이 있으면 `plan.md`의 작업 기록과 변경 기록을 업데이트한다.
- 판단 기준이 애매하거나 사용자에게 영향을 크게 주는 결정은 `docs/agents/constitution-agent.md`의 절차로 검토한다.
- 변경 후에는 `docs/agents/review-agent.md`의 체크리스트로 코드, 의사결정, 문서 업데이트 여부를 검토한다.
