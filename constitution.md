# fss-monitor Constitution

## 1. 목적

이 문서는 `fss-monitor`에서 중요한 결정을 내릴 때 사용하는 판단 기준이다. `plan.md`는 프로젝트의 의도와 변경 이력을 저장하고, 이 문서는 무엇을 선택해야 하는지 애매할 때 기준을 제공한다.

## 2. 핵심 원칙

1. 원래 목적을 보존한다.
   - 이 프로젝트는 금감원 제재 공시를 놓치지 않고 감지하고, 기사 작성과 만평 제작, CMS 입력까지 이어지는 실무 흐름을 줄이기 위한 것이다.
   - 단순 링크 알림 모니터라는 현재 상태를 유지하되, 확장 목표를 잊지 않는다.

2. 사실성과 출처를 우선한다.
   - 기사 초안은 금감원 공시 PDF와 원문 링크를 근거로 해야 한다.
   - PDF에서 확인되지 않은 사실을 단정하지 않는다.
   - Gemini 결과는 초안으로 취급하고, 법률/징계 해설의 최종 판단은 사람이 확인할 수 있게 한다.
   - Gemini Gems를 사용할 때는 품질을 우선해 기본 모델을 "빠른 모델"이 아니라 `Pro`로 둔다.

3. 기본값은 안전한 승인요청 저장이다.
   - moneynlaw.co.kr 입력 자동화의 기본 결과는 `기사검토=승인요청` 상태로 저장하는 것이다.
   - `승인요청` 상태의 `저장하기`는 최종 공개 발행이 아니라 승인 대기 단계로 보며, 기사와 이미지 입력이 완료되면 추가 사용자 확인 없이 진행할 수 있다.
   - 즉시 발행, 예약 발행, 승인 완료, 기존 기사 공개 수정, 삭제 같은 행위는 사용자의 명시적 승인 없이는 하지 않는다.

4. 비밀과 세션을 저장소에 남기지 않는다.
   - 로그인 정보, 쿠키, 세션 토큰, 웹훅, API 키, CMS 비밀번호는 커밋하지 않는다.
   - 인증은 사용자가 로그인한 로컬 브라우저 프로필 또는 안전한 환경변수/키체인 방식으로 처리한다.
   - moneynlaw.co.kr 로그인은 가능한 한 관리자 로그인보다 회원 로그인 화면을 우선 사용한다.

5. 자동화는 단계별로 검증한다.
   - PDF 다운로드, 기사 생성, 만평 생성, CMS 입력은 각각 독립적으로 검증 가능해야 한다.
   - 실패한 단계 이후를 무리하게 진행하지 않는다.
   - 재실행 시 같은 공시가 중복 처리되거나 중복 발행되지 않도록 상태를 남긴다.

6. 사용자 영향이 큰 변화는 먼저 묻는다.
   - 자동 발행 활성화
   - 알림 빈도/대상 채널 변경
   - moneynlaw 사이트에 공개 상태로 저장되는 작업
   - Gemini Gems 대신 다른 모델/API로 바꾸는 결정
   - 기존 모니터의 원격 실행 구조를 크게 바꾸는 결정
   - 저장소에 새 외부 의존성이나 브라우저 자동화 권한을 도입하는 결정

7. 로컬 UI 자동화의 한계를 숨기지 않는다.
   - Gemini Gems와 CMS 웹 UI 조작은 Mac이 켜져 있고, 로그인되어 있으며, 필요한 권한이 허용되어야 안정적으로 동작한다.
   - UI 변경으로 자동화가 깨질 수 있으므로 실패 로그와 수동 확인 경로를 제공한다.

8. 파일 삭제는 `trash`를 사용한다.
   - 이 저장소에서 파일 삭제가 필요할 때 `rm`을 사용하지 않는다.
   - `trash`를 사용할 수 없다면 삭제 작업은 멈추고 사용자에게 확인한다.

9. 기기 이동이 필요하면 인계 패키지를 추적한다.
   - `runs/`, `pipeline_state.json`, `service_config.local.json`은 기본적으로 로컬 전용으로 둔다.
   - 다만 사용자가 다른 기기에서 이어가야 한다고 명시하면, 비밀이 없는 산출물만 `handoff/` 같은 추적 경로로 승격해 GitHub에 저장할 수 있다.
   - 이때도 로그인 세션, 쿠키, 비밀번호, 계정 힌트 같은 민감 정보는 커밋하지 않는다.

## 3. 의사결정 규칙

- 선택지가 여러 개라면 기존 프로젝트 구조와 운영 안정성을 우선한다.
- 법률/징계/기사 내용의 정확성에 영향을 주는 변경은 근거와 검증 방법을 함께 남긴다.
- Gemini 모델 모드는 사용자가 달리 지시하지 않는 한 `Pro`를 기본값으로 삼는다.
- moneynlaw CMS 작업은 사용자가 달리 지시하지 않는 한 Chrome 프로필 `가리봉동`과 회원 로그인 화면을 사용한다.
- `기사검토=승인요청` 저장은 안전한 기본 단계로 보고, 입력과 이미지 첨부가 완료되면 `저장하기`까지 진행한다.
- 사용자의 계정, 공개 발행, 비용, 외부 API 호출이 얽히면 자동으로 진행하지 말고 승인 여부를 확인한다. 단, 사용자가 지정한 로컬 브라우저 프로필의 저장된 로그인 정보를 사용하는 것은 이 워크플로의 기본 전제다.
- 실험적 자동화는 먼저 로컬 산출물 생성 또는 CMS 승인요청 저장까지로 제한한다.
- 판단 기준이 충돌하면 다음 우선순위를 따른다.
  1. 사용자 명시 지시
  2. 보안/비밀 보호
  3. 공개 발행 안전성
  4. 사실성과 출처
  5. 운영 안정성
  6. 편의성과 자동화 범위

## 4. constitution agent 호출이 필요한 경우

아래 상황에서는 `docs/agents/constitution-agent.md` 절차에 따라 먼저 판단한다.

- 자동 발행 여부를 결정해야 할 때
- Gemini Gems 대신 API나 다른 모델을 쓰는 방향으로 바꾸려 할 때
- moneynlaw.co.kr에 공개 상태로 저장되는 변경을 만들 때
- 로그인/세션/비밀 정보 처리 방식이 바뀔 때
- 기존 GitHub Actions 중심 모니터링 구조를 바꿀 때
- 사용자가 요구한 원래 워크플로와 구현 편의가 충돌할 때
- 새 기능이 `plan.md`의 원래 기획 의도를 바꿀 가능성이 있을 때

## 5. review agent 확인이 필요한 경우

아래 상황에서는 변경 후 `docs/agents/review-agent.md` 체크리스트로 검토한다.

- 코드 또는 워크플로 파일이 변경되었을 때
- `plan.md`, `constitution.md`, agent 문서가 변경되었을 때
- 외부 서비스 연동, 알림, 스케줄, 상태 파일 구조가 변경되었을 때
- 브라우저 자동화 또는 CMS 입력 흐름이 변경되었을 때
- 중요한 의사결정이 있었는지 확인해야 할 때
